import 'package:flutter/material.dart';

class ChatbotFragment {
  /* TextEditingController userId = new TextEditingController();
  TextEditingController password = new TextEditingController();

  void buildConversation() {
    dynamic conversationObject = {'appId': '2eb305af7a1c9612fad0dbeb8ad9be181'};

    KommunicateFlutterPlugin.buildConversation(conversationObject).then((result) {
      print("Conversation builder success : " + result.toString());
    }).catchError((error) {
      print("Conversation builder error occurred : " + error.toString());
    });
  } */
}
